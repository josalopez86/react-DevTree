# react-DevTree
Working with TypeScript, Express, MongooseDb, MongoDb and Rest.

## Configuration

1. Clone .env.template and rename it to .env
2. Install dependencies `npm install`
3. To run the app `npm run dev`

